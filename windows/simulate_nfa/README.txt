要求：
1、输入NFA（不含空转换和含有空转换）和一个符号串，NFA用二维矩阵表示，从文件中输入。
2、若能匹配字符串，则输出识别的过程和Yes。若不能，则输出No。

使用：
simulate_dfa nfa文件 输入字符串

实现：
此代码可以根据不含空转换的NFA，建立对应的NFA状态转移矩阵。代码使用回溯算法，特别的是在
建立NFA转移矩阵的时候，就已经将输入字符与状态对应在了一起，所以不需要剪枝函数。测试用例
用的是课本P61的例题。
