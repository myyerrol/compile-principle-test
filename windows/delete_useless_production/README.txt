使用：
此份代码在开始运行的时候有两种选择，选择1是使用程序事先内建好的输入文法数据（书上P32
的例题），选择2则是自定义输入文法（注意在输入Vn和Vt的数据时，不要有空格）。经过多次自定
义文法测试，代码均能消除无用产生式，算法实现基本完成。若有bug，还需日后完善。

其他：
此份代码最初在Ubuntu-14.04上用gcc-4.8.2编译顺利通过，但是在VS2010上编译时会有警告出现。

实现：
1、产生式是用自定义的结构体形式存储的。
2、集合的描述是用嵌入到结构体中的字符数组实现的。
3、第一个算法循环在Vn不在增大时结束，第二个则在Vn和Vt都不再增大时结束。
